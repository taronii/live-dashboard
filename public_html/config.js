// config.js - production
// Define the backend API URL so that Socket.IO connects correctly on Render.
// If you change the Web Service URL, update this value and redeploy.
window.API_URL = 'https://live-dashboard-api.onrender.com';
